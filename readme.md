# Vizabi Tools DDF-CSV Reader : Angular2 version

## Starting project locally
You will need globally installed `typescript` and `webpack`, `webpack-dev-server`
for webpack sample.

```bash
npm i
npm start
# now you can open http://localhost:3000/
```
